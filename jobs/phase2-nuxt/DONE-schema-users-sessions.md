# Completed: Users/sessions/magic-link-tokens schema sub-files
**Task:** schema-users-sessions.md | **Status:** done | **Date:** 2026-04-19

## Changes Made
- `app/server/db/schema/users.ts:1-19` — new sub-file: `users` table (id, email unique, emailHash, createdAt, lastLoginAt, deletedAt) + `index().on(emailHash)`. Style matches `jobs.ts` / `mapping_cache.ts`.
- `app/server/db/schema/sessions.ts:1-26` — new sub-file: `sessions` table (id, userId FK to users with `onDelete: 'cascade'`, tokenHash unique, createdAt, expiresAt, revokedAt, ipAddress, userAgent) + indexes on `userId` and `expiresAt`. Imports `users` from `./users`.
- `app/server/db/schema/magic_link_tokens.ts:1-21` — new sub-file: `magicLinkTokens` table (id, email, tokenHash unique, createdAt, expiresAt, consumedAt, ipAddress) + composite index on `(email, expiresAt)`. No FK (pre-account-creation tokens).
- `app/server/db/schema.ts:4-9` — barrel: appended 3 `export *` lines (users, sessions, magic_link_tokens). Existing comment + 3 prior exports left untouched.
- `app/drizzle/0001_nebulous_malcolm_colcord.sql` — generated by `drizzle-kit generate`. Creates 3 tables, 1 FK, 4 indexes.
- `app/drizzle/meta/0001_snapshot.json` + `app/drizzle/meta/_journal.json` — drizzle-kit journal update.

## Acceptance Criteria Check
- [x] `users` columns + `index().on(emailHash)` per spec.
- [x] `sessions` columns + FK with `onDelete: 'cascade'` + indexes on userId and expiresAt.
- [x] `magic_link_tokens` columns + composite `(email, expiresAt)` index; no separate `tokenHash` index (covered by unique).
- [x] Barrel updated with 3 re-exports, comment + existing exports preserved.
- [x] Migration generated and committed (`0001_nebulous_malcolm_colcord.sql`).
- [x] `npm run db:migrate` NOT run (per spec — operator task).
- [x] `cd app && npx nuxi typecheck` exit 0.
- [x] `cd app && npm run build` exit 0 (build complete, no type errors).
- [x] `grep "from './users'"` in `sessions.ts` returns the FK import.
- [x] English-only identifiers and comments.
- [x] Each new sub-file under 50 lines.
- [x] Only allowed files touched; no edits to `jobs.ts`, `mapping_cache.ts`, `ai_usage.ts`, `client.ts`, `drizzle.config.ts`, or `package.json`.

## Security Check
- [x] All DB access goes through Drizzle (or parameterized `sql` template) — schema-only task; no handlers added.
- [x] Every mutation endpoint is CSRF-protected — N/A (no endpoints).
- [x] Every job endpoint calls `assertJobAccess` — N/A.
- [x] Every admin endpoint calls `assertAdminSession` + writes to `admin_audit_log` — N/A.
- [x] All inputs Zod-validated (body + query + params) — N/A (no handlers).
- [x] No PII in logs (emails hashed, CIFs redacted, file contents never logged) — schema reserves `email_hash` column for SHA-256 hex; plaintext token never stored (only `token_hash`).
- [x] Session cookies are HttpOnly + Secure + correct SameSite — N/A (cookie wiring is `auth-user` task); schema only stores `tokenHash`, not plaintext.
- [x] Rate limits applied where the task spec requires — N/A (rate limit lookup index `(email, expiresAt)` provided on `magic_link_tokens` for `auth-magic-link` task).

## Commits
- `a510a58` feat(db): add users, sessions, magic_link_tokens schema sub-files
- `514c969` sql(db): generate migration 0001 for users, sessions, magic_link_tokens

## Notes
- Drizzle config glob `./server/db/schema/*.ts` picks up sub-files directly; the barrel re-export is for app-side imports of `~/server/db/schema`.
- No Co-Authored-By trailer on either commit (per CLAUDE.md non-negotiable rule).
