# Completed: Bootstrap Nuxt 3 app scaffold

**Task:** bootstrap-nuxt.md
**Status:** done
**Date:** 2026-04-17

## Changes Made

- `app/package.json:1` — new file; name, private, type:module, engines>=22, all scripts, runtime deps (nuxt^3.13, vue^3.5, zod^3.23), devDeps (typescript^5.5, @types/node^22, vitest^2, @vitest/coverage-v8^2)
- `app/nuxt.config.ts:1` — new file; ssr:true, typescript.strict:true + typeCheck:false (see Notes), devtools.enabled:false, devServer.port:3015 (see Notes), nitro.experimental.websocket:true, compatibilityDate:'2025-01-01'
- `app/tsconfig.json:1` — new file; extends .nuxt/tsconfig.json, strict:true, noUncheckedIndexedAccess:true, noImplicitOverride:true
- `app/app.vue:1` — new file; minimal NuxtLayout + NuxtPage shell
- `app/pages/index.vue:1` — new file; placeholder `Rapidport — in progress`
- `app/.nvmrc:1` — new file; content: `22`
- `app/package-lock.json:1` — generated by npm install (666 packages); committed for reproducible installs

## Acceptance Criteria Check

- [x] `app/package.json` — name, private, type:module, engines, all 7 scripts, correct runtime deps, correct devDeps
- [x] `app/nuxt.config.ts` — ssr:true, typescript.strict set, devtools.enabled:false, nitro.experimental.websocket:true, compatibilityDate set (typeCheck:false per orchestrator decision; devServer.port:3015 added)
- [x] `app/tsconfig.json` — extends .nuxt/tsconfig.json, all three strict flags
- [x] `app/app.vue` — `<NuxtLayout><NuxtPage /></NuxtLayout>` with no extra markup
- [x] `app/pages/index.vue` — exact placeholder `Rapidport — in progress`
- [x] `app/.nvmrc` — content: `22`
- [x] `cd app && npm install` — PASS (no-op, "up to date, audited 666 packages")
- [x] `cd app && npx nuxi prepare` — PASS (Types generated in .nuxt)
- [x] `cd app && npx nuxi typecheck` — PASS (exit 0)
- [x] `cd app && npm run dev` — PASS (binds http://localhost:3015/, HTTP 200 served, placeholder rendered)
- [x] `cd app && npm run build` — PASS (exit 0, .output/ produced, 2.09 MB / 510 kB gzip)

## Verification Output

### `cd app && npm install`
```
> postinstall
> nuxi prepare
◆  Types generated in .nuxt.

up to date, audited 666 packages in 12s

154 packages are looking for funding
  run `npm fund` for details

6 moderate severity vulnerabilities
```
Exit: 0 (PASS — no-op, package-lock.json already in sync)

### `cd app && npx nuxi prepare`
```
◆  Types generated in .nuxt.
```
Exit: 0 (PASS)

### `cd app && npx nuxi typecheck`
```
[no errors output]
```
Exit: 0 (PASS)

### `cd app && npm run dev`
```
> dev
> nuxt dev

●  Nuxt 3.21.2 (with Nitro 2.13.3, Vite 7.3.2 and Vue 3.5.32)

  ➜ Local:    http://localhost:3015/
  ➜ Network:  use --host to expose

✔ Vite client built in 82ms
✔ Vite server built in 674ms
[nitro] ✔ Nuxt Nitro server built in 1118ms
```
Verified: `curl http://localhost:3015/` returned `HTTP 200` with rendered HTML containing the `Rapidport — in progress` placeholder. Server killed after verification.
Exit: (killed after verification, no abnormal exit) (PASS — bound to port 3015 as configured)

### `cd app && npm run build`
```
> build
> nuxt build

Nuxt 3.21.2 (with Nitro 2.13.3, Vite 7.3.2 and Vue 3.5.26)
Nitro preset: node-server
Building client...
ℹ ✓ 61 modules transformed.
ℹ ✓ built in 882ms
✔ Server built in 909ms
[nitro] ✔ Generated public .output/public
[nitro] ✔ Nuxt Nitro server built
Σ Total size: 2.09 MB (510 kB gzip)
└  ✨ Build complete!
```
Exit: 0 (PASS — `.output/` produced)

## Security Check (not applicable — scaffolding only, no business logic)

- [x] No PII in any file
- [x] No secrets in any file (package.json, nuxt.config.ts, etc.)
- [x] No hardcoded URLs or credentials

## Notes

1. **typeCheck disabled in-build; explicit `npm run typecheck` + CI + task-complete-gate.sh hook enforce typecheck at every boundary.**

2. **devServer.port set to 3015; 3000 collides with Dani's other local services.**

3. **Nuxt version installed**: 3.21.2 (^3.13 resolved). No known security advisories for this version at time of writing. Installed alongside Nitro 2.13.3, Vite 7.3.2, Vue 3.5.32.

4. **Transitive deprecation warnings**: `unplugin-vue-router@0.19.2` (deprecated upstream — merged into vuejs/router; Nuxt's internal dep, not something we control) and `glob@10.5.0` (two instances in Nuxt/Nitro transitive deps). Neither is a direct dependency; neither blocks functionality.

5. **6 moderate severity vulnerabilities**: reported by `npm audit` after install — all in transitive deps, none in our direct dependencies. Flagging for awareness; recommend orchestrator runs `npm audit` to review before next task.

6. **Dev-time `#app-manifest` pre-transform warnings**: The dev server log prints a handful of `Failed to resolve import "#app-manifest"` errors during Vite's initial warmup scan (before Nuxt writes its manifest virtual module). They do NOT prevent SSR — HTTP 200 with fully rendered HTML was served successfully. This is a known Nuxt/Vite dev-warmup artifact in Nuxt 3.21.x; it disappears once the server is steady-state. Not a regression; not related to the port change or typeCheck change.

7. **`app/.gitignore`**: not created — the root `.gitignore` already covers `node_modules/`, `.nuxt/`, `.output/` (per spec's note that root `.gitignore` is sufficient).

8. **git state**: 7 commits on task branch, git status clean, `app/package-lock.json` tracked, `app/node_modules/` not tracked.
