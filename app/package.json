{
  "name": "rapidport-app",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxi prepare",
    "typecheck": "nuxi typecheck",
    "lint": "echo 'ESLint not configured yet — see bootstrap-eslint task'"
  },
  "dependencies": {
    "@fontsource/inter": "^5.2.8",
    "@fontsource/jetbrains-mono": "^5.2.8",
    "nuxt": "^3.13",
    "vue": "^3.5",
    "zod": "^3.23"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@vitest/coverage-v8": "^2",
    "typescript": "^5.5",
    "vitest": "^2"
  }
}
